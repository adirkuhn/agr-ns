<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="pageLoaded">
	<ScrollView>
		<StackLayout>
			<Image src="res://logo_agripa_completo" stretch="none" horizontalAlignment="center" />

			<TextField id="email_address" text="{{ email }}" hint="Email Address" keyboardType="email" autocorrect="false" autocapitalizationType="none" />
			<TextField id="password" secure="true" text="{{ password }}" hint="Password" />

			<GridLayout>
				<Button text="Entrar" tap="signIn" cssClass="btn btn-login" />
				<ActivityIndicator busy="{{ authenticating }}" />
				<Button text="Cadastre-se" tap="signIn" cssClass="btn btn-cadastro" />
			</GridLayout>

			<GridLayout>
				<Button text="Entrar" tap="signIn" cssClass="btn btn-login" />
				<ActivityIndicator busy="{{ authenticating }}" />
				<Button text="Cadastre-se" tap="signIn" cssClass="btn btn-cadastro" />
			</GridLayout>
		</StackLayout>
	</ScrollView>
</Page>